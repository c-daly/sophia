# Overlay for containers/docker-compose.test.yml
# Sophia runs from source (like Apollo), not in a container
# This file intentionally left minimal - only defines shared volumes if needed

volumes:
  sophia_test_media_storage: {}
