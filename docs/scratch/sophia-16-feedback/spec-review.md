# Spec Review: Feedback Emission Design

**Spec**: `docs/plans/2026-01-01-feedback-emission-design.md`
**Date**: 2026-01-01

## Checklist Validation

| Criterion | Status | Evidence |
|-----------|--------|----------|
| No pronouns without referents | PASS | Clear: "Sophia emits", "dispatcher.emit()", "worker catches" |
| No vague quantities | PASS | Specific: "5 attempts max", "2^attempts seconds", "10,000 messages" |
| No implied behavior | PASS | All behaviors explicit with processing steps |
| Concrete examples | PASS | Each behavior (B1-B3) has input/output examples |
| Explicit edge cases | PASS | E1-E6 cover Redis down, Hermes down, validation failure, queue overflow |
| Defined interfaces | PASS | Full Python code for all 5 components with types |
| Testable success criteria | PASS | 7 criteria, each with corresponding test in Testing Strategy |

**Checklist Result:** 7/7 passed

## Implementer Dry-Run

### Behaviors Traced

| Behavior | Target File | Implementable? | Gaps |
|----------|-------------|----------------|------|
| B1: Emit observation feedback | `api/app.py` (ingest endpoint) | YES | None - endpoint exists, add emit call |
| B2: Emit plan creation feedback | `api/app.py` (plan endpoint) | YES | None - endpoint exists, add emit call |
| B3: Emit execution feedback | `api/app.py` (execute endpoint) | YES | Need to verify `execution_id` is generated |

### Questions for Spec Author

1. **ExecuteRequest execution_id**: B3 output shows `execution_id="exec-001"` but ExecuteRequest input doesn't have one. Is `execution_id` generated by the execute endpoint, or should it be added to ExecuteRequest?

2. **Worker startup**: How is FeedbackWorker started? The spec shows `async def start()` but doesn't specify where in app startup this is called. Should it be a FastAPI lifespan event?

3. **pydantic-settings vs pydantic**: FeedbackConfig uses `BaseModel` but should use `BaseSettings` to read from env vars. Minor implementation detail.

### Implicit Dependencies Found

1. **redis package** - Listed in pyproject.toml modifications ✓
2. **httpx package** - Already in sophia dependencies (used by worker)
3. **respx package** - Needed for mock_hermes fixture in tests

**Dry-Run Result:** NEEDS CLARIFICATION (2 questions)

## Summary

The spec is comprehensive with:
- Full component code for all 5 modules
- Clear behavior specifications with examples
- Explicit edge case handling
- Testable success criteria

**Minor gaps to clarify before implementation:**
1. Where execution_id comes from in B3
2. How worker starts with FastAPI lifecycle

## Recommendation

**Status: NEEDS CLARIFICATION** on 2 minor points. These can likely be resolved by implementer (generating execution_id in endpoint, using FastAPI lifespan), but confirming avoids rework.
